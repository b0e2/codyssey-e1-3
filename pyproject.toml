[project]
name = "mini-npu-simulator"
version = "1.0.0"
description = "AI가 계산하는 방식을 흉내 내는 작은 계산기 - MAC 연산 기반 Mini NPU 시뮬레이터"
requires-python = ">=3.8"
# 외부 라이브러리 사용 금지: 표준 라이브러리(json, time 등)만 사용
dependencies = []

[tool.mini-npu]
entry = "main.py"
data = "data/data.json"
